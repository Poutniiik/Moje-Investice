name: Denní Report Portfolia

on:
  schedule:
    # 6:00, 16:00, 22:00 (v našem čase - UTC+1/+2)
    # UTC časy: 5:00, 15:00, 21:00
    - cron: '0 5,15,21 * * *' 
  workflow_dispatch:

# Dáváme robotovi právo zapisovat do souborů (Commit & Push)
permissions:
  contents: write

jobs:
  run-report:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install libraries
      # Důležité: Instalujeme vše z requirements.txt (aby tam byl PyGithub)
      run: |
        python -m pip install --upgrade pip
        pip install streamlit>=1.28.0
        pip install yfinance>=0.2.40
        pip install plotly
        pip install PyGithub
        pip install matplotlib
        pip install requests
        pip install requests-cache
        pip install streamlit-lottie
        pip install google-generativeai
        pip install feedparser
        pip install fpdf
        pip install extra-streamlit-components
        pip install kaleido
        pip install wordcloud
        pip install prophet
        pip install plaid-python
        pip install gTTS
        pip install streamlit-mic-recorder
        pip install pandas
        pip install SpeechRecognition

    - name: Run Robot
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        # Token pro stahování a nahrávání dat
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: python daily_bot.py

    # Uložíme změny v CSV (Historie), JSON (Cache) a ZÁLOHY zpět na GitHub
    - name: Commit and Push changes
      run: |
        git config --global user.name 'Investicni Robot'
        git config --global user.email 'robot@github.com'

        # 1. Pokud jsou nějaké změny, na chvíli je schováme do "skříně"
        git stash || echo "Nic ke schování"
        
        # 2. Teď si v klidu stáhneme změny z webu
        git pull --rebase -X ours origin main
        
        # 3. Vytáhneme naše věci zpátky ze skříně
        git stash pop || echo "Nic k vytažení"

        # 4. Teď teprve přidáme soubory k odeslání
        git add value_history.csv || echo "value_history.csv beze změny"
        git add market_cache.json || echo "market_cache.json beze změny"
        git add backups/ || echo "Složka backups/ dnes nevznikla"
        
        # 5. Commit a Push
        git commit -m "💾 Auto-save: Report + Záloha (Time Machine)" || exit 0
        git push origin main
