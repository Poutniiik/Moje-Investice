name: Syntax Police 🚓

on: [push, pull_request]  # Spustí se pokaždé, když něco uložíš

jobs:
  kontrola-kodu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: pip install flake8
        
      - name: Hledání chyb (Syntax Errors)
        # E9,F63,F7,F82 = kódy pro fatální chyby (překlepy, nedefinované proměnné)
        # --select=E9,F63,F7,F82 --show-source --statistics
        run: |
          pip install flake8
          # Zkontroluj všechny .py soubory, ignoruj složité formátování, hledej jen pasti
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
