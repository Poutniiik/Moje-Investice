name: Unit Tester 🧪

on: [push, pull_request]

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        # ZMĚNA: Odstranili jsme "if [ -f ... ]". Teď to instalujeme natvrdo.
        # Pokud requirements.txt chybí nebo je jinde, spadne to už tady a hned uvidíme chybu.
        run: |
          python -m pip install --upgrade pip
          pip install pytest
          pip install -r requirements.txt

      - name: Run Pytest
        run: |
          # Spustí všechny soubory ve složce 'tests'
          pytest
