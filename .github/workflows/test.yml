name: Unit Tester 🧪

on: [push, pull_request]

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        # Potřebujeme knihovny pro testy (pytest) A pro kód (pandas, yfinance, google-generativeai)
        run: |
          pip install pandas yfinance pytest google-generativeai matplotlib

      - name: Run Pytest
        run: |
          # Spustí všechny soubory ve složce 'tests'
          pytest
