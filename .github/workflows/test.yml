name: Unit Tester 🧪

on: [push, pull_request]

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        # OPRAVA: Místo ručního vypisování instalujeme vše z requirements.txt.
        # Tím se automaticky nainstaluje i chybějící 'PyGithub' a 'plaid-python'.
        run: |
          python -m pip install --upgrade pip
          pip install pytest
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Pytest
        run: |
          # Spustí všechny soubory ve složce 'tests'
          pytest
